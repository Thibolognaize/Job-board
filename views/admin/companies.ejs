<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Companies</title>
    <link rel="stylesheet" href="/css/bootstrap.css">
    <link rel="stylesheet" href="/css/fonts.css">
    <link rel="stylesheet" href="/css/global.css">
</head>

<body>
    <section class="container p-3">
        <h1 class="text-center mb-3">Admin - Companies</h1>
        <table class="table table-striped table-bordered table-hover table-sm table-responsive">
            <thead>
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Name</th>
                    <th scope="col">Website</th>
                    <th scope="col">Contact</th>
                    <th scope="col">Adress</th>
                    <th scope="col">City</th>
                    <th scope="col">Siret</th>
                    <th scope="col">User_id</th>
                    <th scope="col">User_email</th>
                </tr>
            </thead>
            <% if (companies && companies.length> 0) { %>
                <tbody>
                    <% companies.forEach(companies=> { %>
                        <tr>
                            <td>
                                <%= companies.id %>
                            </td>
                            <td>
                                <a href="/admin/company/<%= companies.id %>">
                                    <%= companies.name %>
                                </a>
                            </td>
                            <td>
                                <a href="<%= companies.website %>">
                                    <%= companies.website %>
                                </a>
                            </td>
                            <td>
                                <%= companies.contact %>
                            </td>
                            <td>
                                <%= companies.address %>
                            </td>
                            <td>
                                <%= companies.city %>
                            </td>
                            <td>
                                <%= companies.num_siret %>
                            </td>
                            <td>
                                <%= companies.user_id %>
                            </td>
                            <td>
                                <%= companies.user_email %>
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
                <% } else { %>
                    <tbody>
                        <tr>
                            <td colspan="11" class="text-danger">No Companies found</td>
                        </tr>
                    </tbody>
                    <% } %>
        </table>
    </section>
</body>

</html>